<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- قراءة الوسائط: الفحص العادي يعتمد على MediaStore والمجلدات المسموح بها -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- الوصول لكامل التخزين مطلوب للفحص العميق للمجلدات (اختياري) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- إشعار تقدم الفحص -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
        لا يوجد أي إذن للإنترنت: التطبيق يعمل بالكامل دون اتصال
        ولا يرفع أي ملف إلى أي خادم خارجي.
    -->

    <application
        android:name=".DeepRecoveryApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.DeepRecovery"
        tools:targetApi="33">

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.DeepRecovery">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager يعمل بالتهيئة الافتراضية -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge" />

        <!--
            إلزامي: خدمة WorkManager الأمامية مُعلَنة في بيان المكتبة بلا
            foregroundServiceType. وبما أننا نمرّر النوع dataSync في
            ForegroundInfo، يرفض النظام التشغيل ويرمي:
              "foregroundServiceType 0x00000001 is not a subset of
               foregroundServiceType attribute 0x00000000"
            ويُرمى هذا الاستثناء على الخيط الرئيسي بشكل غير متزامن، فلا
            يمكن التقاطه حول استدعاء setForeground — الحل الوحيد هو دمج
            النوع هنا.
        -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>
